# 서버 환경에서 사용하는 정보들은 여기에 설정한다.
spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/coffee_application?serverTimezone=Asia/Seoul
    username: coffee
    password: '010614'